# 實作說明：<單元名稱>（<章節/單元>）

## 對應原始碼

- 單元路徑：`<chapter>/<unit>/`
- 單元概念：`<chapter>/<unit>/README.md`
- 實作檔案（列出此單元所有語言/版本）：
  - `python/<...>.py`
  - `cpp/<...>.cpp`
  - `c/<...>.c`
  - `java/<...>.java`
  - `javascript/<...>.js`
  - `csharp/<...>.cs`
- 文件路徑：`docs/implementations/<chapter>/<unit>/README.md`

## 目標與背景

- 本實作示範的概念：
- 適用情境/常見題型：
- 先修知識（若有）：

## 如何執行

> 請用「可直接貼上終端機」的方式提供指令，並標註需要先 `cd` 到哪裡。若同單元有多語言，建議各語言各放一小節。

```bash
cd <chapter>/<unit>/<language>
<run-command>
```

## 核心做法（重點步驟）

1.
2.
3.

## 關鍵公式/定義（如適用）

- 公式：
- 變數意義：
- 輸入/輸出維度：

## 程式碼區段（節錄 + 解釋）

> 僅節錄「最關鍵」的區段；避免貼整個檔案。若同單元有多版本（manual/numpy），可各節錄一小段對照。

```text
<paste-small-code-snippet-here>
```

- 這段在做什麼：
- 為什麼這樣寫：
- 常見陷阱/邊界情況：

## 驗證方式與預期輸出

- 你可以檢查：
- 預期特性（例如正交、對稱、投影矩陣冪等）：
- 浮點誤差容忍（若有）：例如 `1e-10`
